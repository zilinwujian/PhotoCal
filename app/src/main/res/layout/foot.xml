<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">


    <ImageView
        android:id="@+id/foot_background"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:src="@drawable/foot_background"
        android:scaleType="fitXY"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        />

    <RelativeLayout

        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true">


        <ImageButton
            android:id="@+id/foot_photo"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:src="@drawable/foot_photo"
            android:scaleType="fitCenter"
            android:background="#00000000"
            android:layout_centerInParent="true"
            />

        <ImageButton
            android:id="@+id/foot_home"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="#00000000"
            android:src="@drawable/foot_home"
            android:scaleType="fitCenter"
            android:layout_toLeftOf="@id/foot_photo"
            android:layout_alignParentBottom="true"
            android:layout_centerInParent="true"
            android:layout_marginRight="20dp"
            android:layout_marginBottom="10dp"
            />

        <ImageButton
            android:id="@+id/foot_user"
            android:layout_width="20dp"
            android:layout_height="24dp"
            android:background="#00000000"
            android:src="@drawable/foot_user"
            android:scaleType="fitCenter"
            android:layout_toRightOf="@id/foot_photo"
            android:layout_alignParentBottom="true"
            android:layout_centerInParent="true"
            android:layout_marginLeft="20dp"
            android:layout_marginBottom="10dp"
            />
    </RelativeLayout>

</RelativeLayout>